# Example .env for local PostgreSQL
PGHOST=localhost
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PGPASSWORD=your_password_here

# Alternatively you can set a full DATABASE_URL (takes priority):
# DATABASE_URL=postgresql+psycopg2://postgres:your_password_here@localhost:5432/postgres
